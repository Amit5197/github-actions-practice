name: reusable build workflow

on:
  workflow_call:
    inputs:
      app_name:
        description: "App Name"
        required: true
        type: string
      environment:
        description: "Deploy Environment"
        required: true
        type: string  # Removed the 'default' string because 'required: true' takes precedence
    secrets:
      docker_token:
        required: true
    outputs:   # Map the workflow outputs to job outputs
      build_version:
        value: ${{ jobs.build.outputs.build_version }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs: # Map the job outputs to step outputs
      build_version: ${{ steps.set-version.outputs.build_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print build info
        run: |
          echo "Building ${{ inputs.app_name }} for ${{ inputs.environment }}"

      - name: verify docker token status
        run: |
          if [ -n "${{ secrets.docker_token }}" ]; then
            echo "Docker token is set: true"
          else
            echo "Docker token is set: false"
          fi

      - name: Generate a version
        id: set-version
        run: |
          # Use standard Linux string cutting to get the first 7 characters safely
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          BUILD_VERSION="v1.0-${SHORT_SHA}"
          echo "build_version=$BUILD_VERSION" >> $GITHUB_OUTPUT
          echo "Generated build version: $BUILD_VERSION"
          # Safe Bash extraction to avoid conflicting with GitHub's ${{ }} expression syntax
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          BUILD_VERSION="v1.0-${SHORT_SHA}"
          echo "build_version=$BUILD_VERSION" >> $GITHUB_OUTPUT
        
