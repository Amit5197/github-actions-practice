name: multi-job
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Building app
        run: echo "Building the app"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Running test
        run: echo "Running tests"
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Deploying
        run: echo "Deploying"